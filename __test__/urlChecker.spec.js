import { checkInput } from '../src/client/js/urlChecker';

describe("Testing the URL checking functionality", () => {
    test("Testing the checkInput() function", () => {
           expect(checkInput).toBeDefined();
})});